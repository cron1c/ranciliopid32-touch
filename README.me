Vorsicht Beta!

To do: 
      
      Home Tab
          -- Brew button
          -- Show Brewtimer when brew is detected (open Window and show timer)
          -- Set brew °C value (click on Input Label to set value?)
          -- Implement getScale from scale.h and show in Arc (map weight to arc angle)
         
      Settings tab
          -- Save (event)
          -- More Settings
          -- Fix + - Button Events for PID 
      
